# ایمیل بازیابی رمز با Resend

## ۱) ساخت حساب Resend (۲ دقیقه)

1. بروید به: **<https://resend.com/signup>**
2. با ایمیل ثبت‌نام و تأیید کنید

## ۲) گرفتن API Key

1. <https://resend.com/api-keys>
2. **Create Token**
3. دسترسی **Sending access**
4. توکن `re_...` را کپی کنید

## ۳) فرستنده (From)

### روش رایگان سریع (تست / شروع)
در همان Resend یک **domain تست** بسازید یا از آدرس onboarding استفاده کنید.

بهتر: اگر دامنه دارید (مثلاً `yourdomain.com`) در **Domains** اضافه کنید و رکوردهای DKIM/SPF را بزنید، سپس:

```
EMAIL_FROM = "گیم‌نت کلاب انرژی <info@yourdomain.com>"
```

> بدون دامنه، Resend فقط به ایمیل خودتان در حین تست اجازه ارسال می‌دهد. برای ارسال به همه مشتری‌ها، دامنه لازم است.

## ۴) ست کردن Secret روی سرور

```bash
cd worker
export CLOUDFLARE_API_TOKEN=cfut_...
export CLOUDFLARE_ACCOUNT_ID=0069c6e1bdc550b6c6c0f18ebd0c195c

printf '%s' 're_XXXXXXX' | npx wrangler secret put RESEND_API_KEY
```

و در `wrangler.toml` (یا به‌صورت var):

```toml
SITE_URL = "https://abadeh777iu-jpg.github.io/gamenet/"
# EMAIL_FROM = "گیم‌نت <info@yourdomain.com>"
```

## ۵) تست

1. سایت را باز کنید → **ورود** → **«فراموشی رمز عبور؟»**
2. ایمیل مشتری را بزنید → **ارسال لینک**
3. لینک `#reset/...` به ایمیل می‌آید → تعویض رمز

اگر ایمیل نرسید، از پنل ادمین 👑 → **لینک ریست** بگیرید و خودتان بفرستید.

---

## پنل سوپر ادمین 👑

روی سایت، وقتی وارد `#admin` شدید، دکمه **👑** پایین‌صفحه ظاهر می‌شود:

| امکان | توضیح |
|-------|--------|
| آمار کلی | فعال / در انتظار / مسدود / منقضی |
| جستجوی مشتری | ایمیل، نام، تلفن |
| **+۳۰ روز** | تمدید لایسنس |
| **مسدود / رفع** | قطع یا وصل دسترسی |
| **لینک ریست** | لینک بازیابی دستی (کپی می‌شود) |
| **ریست رمز** | تعویض رمز مشتری از پنل |
| **تغییر رمز ادمین** | عوض کردن `admin123` |

ورود پنل: کلید ادمین (پیش‌فرض `admin123`)

مستقیم: `https://abadeh777iu-jpg.github.io/gamenet/#super`
