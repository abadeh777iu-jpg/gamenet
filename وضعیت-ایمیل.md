# وضعیت ایمیل بازیابی رمز

## ✅ انجام شد

| مورد | وضعیت |
|------|--------|
| سری `RESEND_API_KEY` روی Worker | ✅ |
| endpoint `/api/forgot` و `/api/reset` | ✅ |
| لینک «فراموشی رمز عبور؟» روی فرم ورود | ✅ |
| صفحه تعویض رمز (`#reset/TOKEN`) | ✅ |
| لینک ریست دستی از پنل 👑 | ✅ |
| ریست رمز مشتری از پنل 👑 | ✅ |
| ارسال ایمیل به صاحب حساب Resend | ✅ تست شد |

## ⚠️ محدودیت حالت تست Resend

توکن شما فعلاً فقط می‌تواند به ایمیل زیر ایمیل بفرستد:

```
abadeh777iu@gmail.com
```

خطای ۴۰۳ برای بقیه آدرس‌ها:

> You can only send testing emails to your own email address…

### راه‌حل: تأیید دامنه (۱۰–۲۰ دقیقه)

اگر **دامنه** دارید (مثلاً `clubenergy.ir` یا `…`)：

1. <https://resend.com/domains> → **Add Domain**
2. رکوردهای DNS که Resend می‌دهد را در پنل دامنه‌تان بزنید
3. بعد از **Verified** شدن، به من بگویید تا `EMAIL_FROM` را مثلاً این‌طور بگذارم:

```
گیم‌نت کلاب انرژی <info@yourdomain.com>
```

بعد از آن، هر مشتری‌ای «فراموشی رمز» بزند، ایمیل به صندوق او می‌رود.

### تا قبل از تأیید دامنه

مشتری می‌تواند از شما لینک بازیابی بگیرد:

1. مشتری پیام می‌دهد «رمز را فراموش کردم»
2. شما: پنل **👑 سوپر ادمین** → دکمه **لینک ریست** کنار نام او
3. لینک کپی می‌شود → برایش می‌فرستید

---

## پنل سوپر ادمین (تکمیلی)

لینک مستقیم: `https://abadeh777iu-jpg.github.io/gamenet/#super`  
یا هنگام ورود با `#admin` → دکمه 👑 پایین‌صفحه

| امکان | توضیح |
|-------|--------|
| آمار | فعال / در انتظار / مسدود / منقضی |
| جستجوی مشتری | ایمیل، نام، تلفن |
| **+۳۰ روز** | تمدید لایسنس |
| **مسدود / رفع** | قطع/وصل دسترسی |
| **لینک ریست** | لینک بازیابی دستی (کپی) |
| **ریست رمز** | تعویض رمز مشتری |
| **تغییر رمز ادمین** | عوض کردن `admin123` |

کلید ورود پنل: `admin123` (پیشنهاد: از داخل خود پنل عوض کنید)
