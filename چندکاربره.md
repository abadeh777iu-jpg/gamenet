# راهنما: سرور چندکاربره + ذخیره روی گیت‌هاب شما

## چه چیزی عوض شد؟

| قبل | بعد |
|------|------|
| دکمه شناور ☁️ دستی | **پاک شد** — همگام‌سازی خودکار و بی‌صدا |
| داده فقط در مرورگر هر دستگاه | داده هر مشتری روی **گیت‌هاب شما** در `data/db/customers/{id}.json` |
| رمز عبور در localStorage متن ساده | رمز روی سرور **هش** می‌شود |
| همه داده‌ها قاطی | **هر کاربر فقط داده خودش** را می‌بیند |

## معماری

```
مرورگر مشتری ──► Cloudflare Worker (سرور شما)
                      │  (توکن گیت‌هاب فقط اینجاست — Secret)
                      ▼
              GitHub مالک (abadeh777iu-jpg/gamenet)
              data/db/users.json
              data/db/customers/{id}.json
```

- **ثبت‌نام/ورود** از طریق Worker انجام می‌شود
- با هر تغییر (میز، جلسه، بوفه…) داده خودکار push می‌شود
- هر ۶۰ ثانیه یا هنگام بازگشت به تب، pull می‌شود

## استقرار سرور

راهنمای کامل: [`worker/استقرار-سرور.md`](worker/استقرار-سرور.md)

خلاصه:

1. حساب رایگان Cloudflare بسازید
2. توکن Cloudflare با مجوز Workers Edit بگیرید
3. اجرا از پوشه `worker/`:

```bash
export CLOUDFLARE_API_TOKEN=...
export CLOUDFLARE_ACCOUNT_ID=...
npx wrangler secret put GITHUB_TOKEN   # توکن گیت‌هاب (همان ghp_…)
npx wrangler deploy
```

4. آدرس خروجی Worker را در `index.html` اینجا بگذارید:

```html
window.GAMENET_API = "https://gamenet-server.....workers.dev";
```

5. فایل‌ها را push کنید — سایت به سرور وصل می‌شود.

## امنیت

- توکن گیت‌هاب هرگز در کد جاوااسکریپت سایت قرار نمی‌گیرد
- رمزها با SHA-256 + نمک هش می‌شوند
- CORS فقط به دامنه سایت شما اجازه می‌دهد
- هر نشست (session) با توکن تصادفی است و با خروج باطل می‌شود

## اگر Worker هنوز مستقر نشده باشد

سایت با ورود/ثبت‌نام **محلی** کار می‌کند (حالت قبلی). به‌محض ست شدن `GAMENET_API`، حالت سرور فعال می‌شود.
