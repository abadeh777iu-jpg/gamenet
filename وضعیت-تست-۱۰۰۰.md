# گزارش نهایی — تست ۱۰۰۰ مشتری

تاریخ: ۲۰۲۶/۰۹/۲۴

## ۱) تغییرات رابط کاربری
- ❌ دکمه «🛡️ ورود مدیر» از پایین صفحه ورود **حذف شد**
- 👑 لینک اختصاصی **«ورود سوپر ادمین»** (`#super`) جایگزین شد
- 🗑 دکمه **حذف گیم‌نت** در پنل سوپر ادمین اضافه شد (`POST /api/admin/delete`)

## ۲) ثبت ۱۰۰۰ مشتری تست
| مورد | مقدار |
|------|-------|
| کل مشتریان | **۱۰۱۳** |
| تست‌ها | `test0001@example.com` … `test1000@example.com` |
| رمز همه | `test1234` |
| وضعیت | در انتظار (pending) — ۱۰۱۱ |
| فعال واقعی | ۲ (مشتریان قبلی شما) |

## ۳) ۱۰۰۰ رسید در انتظار تأیید (پلن ترکیبی)
| پلن | قیمت | تعداد (تقریبی) |
|-----|------|----------------|
| ماهانه | ۵۰۰,۰۰۰ | ~۳۳۳ |
| سالانه | ۵,۰۰۰,۰۰۰ | ~۳۳۳ |
| طلایی | ۹,۰۰۰,۰۰۰ | ~۳۳۳ |

- همه `status=pending`
- اندپوینت: `GET/POST /api/admin/payments`
- پنل مالک خودکار `gc_payments` را pull/push می‌کند (`auto-sync.js?v=5`)

## ۴) نحوه تأیید پرداخت توسط شما
1. وارد سایت شوید → لینک **👑 ورود سوپر ادمین** (پایین صفحه) یا `#admin`
2. سربرگ **💳 پرداخت‌های در انتظار** را باز کنید
3. هر رسید را **تأیید** یا **رد** کنید (تأیید → فعال‌سازی لایسنس)
4. تغییرات روی سرور ذخیره می‌شود

## ۵) نتایج تست فشار (با کش Worker)
| تست | نتیجه | p50 | p95 |
|-----|-------|-----|-----|
| health ×300 (۵۰ همزمان) | **300/300** | ~۱۰۰ms | ~۱۷۰ms |
| stats ×40 | **40/40** | ~۲۳۰ms | ~۱.۱s |
| customers ×20 | **20/20** | ~۲۷۰ms | ~۱.۱s |
| payments ×20 | **20/20** | ~۵۷۰ms | ~۹۰۰ms |
| login متوالی ×15 | **15/15** | ~۳.۲s | — |
| login همزمان ×6 | **6/6** | — | — |

> لاگین چون روی GitHub commit می‌زند کمی کند است؛ زیر بار شدید همزمان ممکن است موقتاً ۵۰۳ بدهد (محدودیت GitHub) — برای استفاده عادی مشکلی نیست.

## ۶) لینک‌ها
- سایت: https://abadeh777iu-jpg.github.io/gamenet/
- Worker: https://gamenet-server.abadeh-gamenet.workers.dev
- SW: `club-energy-v6`
