# ایمیل بازیابی رمز با Resend

## ۱) ساخت حساب (۲ دقیقه)

1. بروید به: **<https://resend.com/signup>**
2. با ایمیل ثبت‌نام کنید و تأیید کنید

## ۲) گرفتن API Key

1. <https://resend.com/api-keys>
2. **Create API Key**
3. دسترسی: **Sending access**
4. متن `re_…` را کپی کنید

## ۳) دامنه فرستنده

### گزینه آ — سریع (تست، فقط به ایمیل خودتان)
- از فرستنده `onboarding@resend.dev` استفاده می‌شود
- فقط به ایمیل صاحب حساب Resend می‌رود (مناسب تست)

### گزینه ب — حرفه‌ای (پیشنهادی برای مشتری‌ها)
اگر **دامنه** دارید (مثلاً `clubenergy.ir`):
1. <https://resend.com/domains> → **Add Domain**
2. رکوردهای DNS را در پنل دامنه‌تان بزنید
3. بعد از Verify، فرستنده را مثل `info@clubenergy.ir` بگذارید

> اگر دامنه ندارید، برای شروع همان `onboarding@resend.dev` را بگذارید؛ بعداً که دامنه گرفتید عوض می‌کنیم.

## ۴) سری را در سرور ذخیره کنید

```bash
cd worker
export CLOUDFLARE_API_TOKEN=cfut_...
export CLOUDFLARE_ACCOUNT_ID=0069c6e1bdc550b6c6c0f18ebd0c195c

printf '%s' 're_XXXX' | npx wrangler secret put RESEND_API_KEY
```

فرستنده (در `wrangler.toml` یا secret):

```bash
printf '%s' 'Gamenet <onboarding@resend.dev>' | npx wrangler secret put EMAIL_FROM
```

یا در `wrangler.toml`:

```toml
[vars]
SITE_URL = "https://abadeh777iu-jpg.github.io/gamenet/"
EMAIL_FROM = "Gamenet <onboarding@resend.dev>"
```

## ۵) تست

در سایت روی صفحه ورود: **«فراموشی رمز عبور؟»** → ایمیل → چک inbox/spam.

---

## خلاصه متغیرها

| متغیر | نوع | توضیح |
|-------|-----|--------|
| `RESEND_API_KEY` | Secret | کلید Resend |
| `EMAIL_FROM` | Var/Secret | فرستنده |
| `SITE_URL` | Var | آدرس سایت برای ساخت لینک |
