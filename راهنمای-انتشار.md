# راهنمای انتشار دائمی «محاسبه‌گر مالی گیم‌نت کلاب انرژی»

این پکیج شامل خود سایت + همگام‌سازی ابری (GitHub) است.

---

## ۱) ساخت حساب GitHub (اگر ندارید)

1. به <https://github.com/signup> بروید
2. ایمیل، رمز و نام کاربری را وارد کنید → Continue
3. ایمیل را تأیید کنید

---

## ۲) ساخت توکن دسترسی (Personal Access Token)

> توکن مثل رمز عبور برنامه است؛ فقط خودتان نگهش دارید.

### روش پیشنهادی: Fine-grained token (جدید)

1. وارد حساب شوید → <https://github.com/settings/personal-access-tokens/new>
2. **Token name**: مثلاً `gamenet-deploy`
3. **Expiration**: ۹۰ روز یا No expiration (به سلیقه شما)
4. **Repository access** → Only select repositories → مخزن ساخته‌شده (بعد از بار اول)
   - برای **بار اول** که هنوز مخزن نیست: گزینه «All repositories» را بزنید
5. **Permissions → Repository permissions** را این‌طوری کنید:
   - **Contents**: Read and write
   - **Pages**: Read and write
   - **Metadata**: Read-only (خودکار)
6. پایین صفحه **Generate token** → کپی کنید (`github_pat_…`)

### روش جایگزین: Classic token

1. <https://github.com/settings/tokens/new>
2. نام دلخواه، انقضا دلخواه
3. تیک **repo** (کل بخش)
4. Generate → کپی (`ghp_…`)

---

## ۳) انتشار (دو راه)

### راه A — خودکار (از من بخواهید)

توکن + نام کاربری را بفرستید؛ من اجرا می‌کنم:

```bash
GITHUB_TOKEN=… GITHUB_USER=… ./deploy.sh gamenet
```

### راه B — دستی با Git

```bash
cd site
git remote add origin https://github.com/USER/gamenet.git
git push -u origin main
```

سپس در GitHub مخزن:
1. Settings → Pages
2. Source: **Deploy from a branch**
3. Branch: **main** / **/ (root)** → Save

آدرس سایت: `https://USER.github.io/gamenet/`

---

## ۴) اتصال فضای ابری در خود اپ

1. سایت منتشرشده را باز کنید
2. دکمه شناور **☁️** (پایین‌چپ) را بزنید
3. **توکن** و **نام مخزن** (`USER/gamenet`) را وارد کنید → **ذخیره اتصال**

از این به بعد:
- هر تغییری در میزها، جلسات، بوفه، پرداختها… خودکار در فایل
  `data/cloud-backup.json` مخزن ذخیره می‌شود
- باز کردن صفحه روی هر دستگاهی، آخرین داده‌ها را از ابر می‌گیرد
- توکن فقط در مرورگر خودتان ذخیره می‌شود؛ در کد سایت نیست

---

## ۵) ساختار پکیج

| فایل | نقش |
|------|-----|
| `index.html` | کل اپ (تک‌فایل React) |
| `cloud-sync.js` | همگام‌سازی با GitHub (فضای ابری) |
| `manifest.json` / `sw.js` | PWA و حالت آفلاین |
| `deploy.sh` | انتشار خودکار با توکن |
| `icon-*.png` / `favicon-*.svg` | آیکون‌ها |
| `robots.txt` | دستور خزنده‌ها |

---

## نکته امنیتی

- توکن را در هیچ فایلی از مخزن کامیت **نکنید**
- اگر لو رفت، همان لفحه <https://github.com/settings/tokens> آن را Revoke کنید
- پنل ☁️ توکن را فقط در `localStorage` مرورگر خودتان نگه می‌دارد
